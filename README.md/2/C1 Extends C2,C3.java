Result: Compile-time error (Class cannot extend multiple classes).

Reason: Java does not support multiple inheritance with classes to avoid ambiguity (the Diamond Problem). A class can only extend one class.
